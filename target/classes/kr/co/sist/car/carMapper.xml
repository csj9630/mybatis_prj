<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="carMapper">
<!--국가로 제조사 조회 -->
<select id="carMakerFromKorea"  resultType="String">
select Maker
from CAR_MAKER
where Maker in ('현대','기아','삼성','쌍용')
</select>
<select id="carMakerFromForeign" resultType="String">
select Maker
from CAR_MAKER
where Maker not in ('현대','기아','삼성','쌍용')
</select>

<select id="carModelFromMaker" parameterType="String" resultType="String">
	select model
	from CAR_MAKER
	where Maker = #{maker}
</select>


<select id="carInfo" parameterType="String" resultType="carDomain">
SELECT cma.maker, cma.model, cmo.car_year, cmo.price,
cmo.cc, cmo.car_img, cmo.input_date

FROM CAR_MAKER cma, CAR_MODEL cmo

where cmo.model=cma.model
and cmo.model=#{model}
order by cmo.input_date desc
</select>

</mapper>

